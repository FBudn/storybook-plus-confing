import { StyleSheet } from "react-native";

const IconsSectionStyles = StyleSheet.create({
  container: {
    flexDirection: "row",
    width: "100%",
    justifyContent: "center",
    alignItems: "center",
    gap: 15,
    margin: 4,
  },
});

export default IconsSectionStyles;
